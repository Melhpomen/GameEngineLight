load.Loading = function()
{
	var list = new Array(); // Contains all images we need to load

	// list["folder"] = ["pic1", "pic2", "pic3", "pic4"];

	load.Preload(list); // We have the list, let's load it
}